name: GitHub Actions Demo
ruin-name: $  {{ github.actor }} is testing out
Github Actions
on: [push]
jobs: 
   Explore-GitHub-Actions:
      runs-on: ubuntu-latest
      steps:
         - name:  eche "The job was automatically triggered by a ${{ github.event_name }} event."
         